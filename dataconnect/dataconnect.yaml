specVersion: "v1beta"
serviceId: "redreport"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "redreport-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
